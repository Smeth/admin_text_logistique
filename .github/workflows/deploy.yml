# .github/workflows/deploy.yml

name: CI/CD Laravel Deployment

on:
  push:
    branches:
      - master

jobs:
  # 1. Continuous Integration (Test & Build)
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.3'
        extensions: bcmath, pdo_mysql, zip, mbstring
        coverage: none

    - name: Cache Composer dependencies
      uses: actions/cache@v4
      with:
        path: vendor
        key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
        restore-keys: ${{ runner.os }}-composer-

    - name: Install Dependencies
      run: composer install --no-dev --prefer-dist


  # 2. Continuous Delivery (Deployment via SSH)
  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: SSH Deploy Command
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.AWS_HOST }}
        username: ${{ secrets.SSH_USERNAME }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          set -e

          echo "🔍 Déplacement dans le dossier du projet..."
          cd /var/www/livrango/admin

          echo "⚙️  Autorisation Git safe.directory..."
          git config --global --add safe.directory /var/www/livrango/admin

          echo "⬇️  Pull du dernier code..."
          git pull origin master

          echo "📦 Installation des dépendances Laravel..."
          composer install --no-dev --prefer-dist

          echo "🗃️  Exécution des migrations..."
          php artisan migrate --force

          echo "🧹 Nettoyage complet du cache Laravel..."
          php artisan optimize:clear
          php artisan cache:clear
          php artisan config:clear
          php artisan route:clear
          php artisan view:clear

          echo "🔁 Redémarrage de PHP-FPM..."
          sudo service php8.3-fpm restart

          echo "🚀 Déploiement terminé avec succès !"
